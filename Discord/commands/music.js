const { Channel, Message } = require("discord.js")
const { description, execute } = require("./help")

module.exports = {
    name: "music",
    description: "Type !help music for the command",
    permission: "everyone"
}